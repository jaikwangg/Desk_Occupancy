# โครงการ: ระบบตรวจวัดการเข้าใช้โต๊ะทำงานด้วย CCTV (Desk Occupancy Monitor)

เอกสารนี้ครอบคลุมทั้งโปรเจกต์ — บริบท, การตัดสินใจด้านสถาปัตยกรรม, วิธีติดตั้ง/รัน,
คู่มือการตั้งค่าโซน, ข้อจำกัด และข้อควรระวังด้านกฎหมาย (PDPA)

เป็น **single source of truth** สำหรับนำไปต่อยอดหรือ deploy จริง

---

## 1. บริบทและโจทย์

- **ผู้ใช้:** Worakij Wanwong (Software Builder, กรุงเทพฯ)
- **โจทย์เดิม:** ใช้ CCTV + motion capture ระบุพฤติกรรมพนักงานว่า "ทำงานอยู่หรือไม่"
- **สภาพแวดล้อม:** โต๊ะ/ออฟฟิศ ยังไม่มี CCTV (จะติดตั้งใหม่)
- **ตำแหน่ง workstation:** fix อยู่แล้ว (ไม่ย้าย) → ตั้งค่าโซนครั้งเดียวแล้วใช้ต่อเนื่อง

---

## 2. การปรับเป้าหมาย (สำคัญ)

ระบบนี้วัด **สถานะการเข้าใช้โต๊ะ (occupancy/activity)** ไม่ใช่ "ประสิทธิภาพการทำงาน"
CCTV พิสูจน์ไม่ได้ว่าพนักงาน "กำลังทำงานจริง" เพราะคนนั่งนิ่งอาจกำลังคิด/อ่าน/คุยโทรศัพท์

MVP จึงตรวจจับเฉพาะ**สถานะเชิงวัตถุวิสัย (objective)** ที่วัดได้จริง:

| สถานะ | ความหมาย | เงื่อนไข |
|---|---|---|
| `PRESENT` | มีคนที่โต๊ะ | พบ person center ภายใน ROI ของโซน |
| `AWAY` | ไม่มีคนที่โต๊ะ | ไม่พบ person ≥ `away_seconds` |

> **หมายเหตุ:** เวอร์ชันเต็ม (`desk_activity_monitor.py`, `desk_multi_monitor.py` ใน `PLAN.md`)
> มีสถานะ `ACTIVE`/`IDLE`/`AWAY` เพิ่ม แต่เนื่องจาก "นิ่ง ≠ ไม่ทำงาน"
> เวอร์ชัน production จึงใช้ `PRESENT`/`AWAY` ตามที่ผู้ใช้ยืนยันว่า "แค่อยากรู้ว่าเขาทำงานอยู่มั้ย"

---

## 3. สถาปัตยกรรม

```
[CCTV กล้อง/โต๊ะ] ──RTSP──▶ [Edge Box (ประมวลผลใกล้กล้อง)]
                                  ├── YOLOv8n: ตรวจจับบุคคลบนเต็มเฟรม
                                  ├── แมป person center → โซน (ROI)
                                  ├── State machine: PRESENT / AWAY ต่อโซน
                                  └── เก็บเฉพาะ event log (CSV/SQLite) ไม่เก็บวิดีโอดิบ
                                          │
                                          ▼
                              [Dashboard + หน้าตั้งค่าโซน]  (Flask, port 8080)
```

### Privacy by Design

- ประมวลผลที่ edge box ไม่ส่งวิดีโอดิบขึ้น cloud
- ไม่ทำ face recognition, ไม่ระบุตัวบุคคล, ไม่วิเคราะห์อารมณ์
- เก็บเฉพาะ log การเปลี่ยนสถานะ ไม่ใช่เฟรมภาพ
- รายงานระดับโซน/โต๊ะ ไม่ใช่รายบุคคล
- ตั้งกล้องครอบคลุมโซนโต๊ะ + privacy masking บริเวณที่ไม่จำเป็น

---

## 4. เทคโนโลยีและการตัดสินใจเลือกใช้

| ส่วน | เทคโนโลยี | เหตุผล |
|---|---|---|
| ตรวจจับบุคคล | **YOLOv8n** (หลัก) | แม่นยำกับคนหลายคน/มุมมองจากด้านบน ตรวจได้ครบ |
| Fallback | MediaPipe Pose → HOG | กรณีไม่มี `ultralytics` |
| วิดีโอ I/O | OpenCV (`opencv-python-headless`) | อ่าน RTSP/webcam/ไฟล์ |
| Dashboard | Flask | แสดงสถานะสด + หน้าตั้งค่าโซน |
| Log | CSV + SQLite | event log ต่อโซน |

### ทำไมใช้ YOLO แทน HOG/MediaPipe Pose

- **HOG** ตรวจคนนั่ง/มุมมองจากด้านบนไม่แม่นยำ (false positive บนของบนโต๊ะ)
- **MediaPipe Pose** ตรวจคนหลายคนในมุมบนได้ไม่ดี และหลอกผิดจากของบนโต๊ะเมื่อ crop
- **YOLO** ตรวจครั้งเดียวบนเต็มเฟรม แล้วแมป center เข้าโซน — ทั้งแม่นยำกว่าและเร็วกว่า

### กลยุทธ์การตรวจจับ (สำคัญ)

ตรวจจับบุคคล **1 ครั้งบนเต็มเฟรม** (downscale เป็น 480px) แล้วเช็คว่า center ของแต่ละคน
ตกใน ROI ของโซนใด — **ไม่ใช่** crop แยกตรวจต่อโซน (crop ทำให้เกิด false positive จากของบนโต๊ะ)

---

## 5. คำแนะนำการเลือกและติดตั้ง CCTV

| เรื่อง | แนะนำ |
|---|---|
| โปรโตคอล | RTSP / ONVIF (Hikvision, Dahua, UBNT, TP-Link) |
| ความละเอียด | 1080p ขึ้นไป |
| ตำแหน่ง | กล้องต่อ 1 โต๊ะ/โซน มุมสูงเล็กน้อย มองเห็นท่านั่ง + มือบนโต๊ะ |
| FPS | 2–5 fps พอสำหรับ occupancy (ประหยัดทรัพยากร) |
| แสง | แสงคงที่ หลีกเลี่ยงม่าน/หน้าต่างกระแทกแสงในกรอบ |
| Edge Box | mini PC / NVR รัน Docker: Python + OpenCV + ultralytics (CPU ได้) |
| Privacy | เปิด privacy masking บริเวณที่ไม่ใช่โซนโต๊ะ, ไม่บันทึกวิดีโอต่อเนื่อง |

---

## 6. โครงสร้างไฟล์

```
cctv-desk-monitor/
├── context.md                      # ไฟล์นี้ — บริบทและคู่มือทั้งหมด
├── desk_occupancy_multi.py         # โปรแกรมหลัก (multi-camera/multi-zone)
├── occupancy.yaml                  # config กล้อง + โซน + dashboard
├── requirements.txt                # dependencies
├── test_pipeline.py                # เทสต์ pipeline (ไม่ต้องมี torch)
├── PLAN.md                         # แผนสถาปัตยกรรมเดิม (เวอร์ชัน ACTIVE/IDLE/AWAY)
├── models/
│   ├── yolov8n.pt                  # โมเดล YOLOv8n (ตรวจจับบุคคล)
│   └── pose_landmarker.task        # MediaPipe Pose (fallback)
└── data/
    ├── sample_real.mp4             # วิดีโอตัวอย่างสำหรับทดสอบ
    ├── dashboard_example.png       # ตัวอย่างหน้า dashboard
    └── calibrate_example.png       # ตัวอย่างหน้าตั้งค่าโซน
```

---

## 7. การติดตั้งและรัน

### ติดตั้ง dependencies

```bash
python -m venv .venv
.venv/Scripts/activate          # Windows  (Linux/mac: source .venv/bin/activate)
pip install -r requirements.txt
```

ultralytics ลาก torch มาด้วย รวมแล้ว venv ~**1.1 GB** ครั้งแรกที่รันจะดาวน์โหลด
`yolov8n.pt` (6.5 MB) ให้อัตโนมัติ

### รันทดสอบ (วิดีโอตัวอย่าง)

```bash
cd cctv-desk-monitor
# threshold สั้นๆ สำหรับทดสอบกับวิดีโอตัวอย่าง
python desk_occupancy_multi.py --config occupancy.yaml --test-away 3
```

### รันกับ RTSP จริง

แก้ `occupancy.yaml` ส่วน `source` เป็น `rtsp://user:pass@IP:554/stream1` แล้ว:

```bash
python desk_occupancy_multi.py --config occupancy.yaml
```

### รันเทสต์

```bash
python test_pipeline.py     # 19 เคส - ใช้ detector ปลอม ไม่ต้องมี torch
```

### เปิด dashboard

เปิดเบราว์เซอร์ที่ <http://localhost:8080/>

### ตั้งค่าโซนโต๊ะ (ครั้งเดียว)

1. หน้า dashboard → กด **"ตั้งค่าโซนโต๊ะ"** หรือเข้า <http://localhost:8080/calibrate>
2. ระบบดึง snapshot จริงจากแต่ละกล้อง
3. **ลากกรอบ** บริเวณที่ตัวคน/เก้าอี้ปรากฏจริงในแต่ละกล้อง
4. กด **"บันทึก & ใช้งาน"** → โซนใหม่ใช้ทันที + บันทึกลง `data/zones_calibrated.json`
   (โหลดอัตโนมัติครั้งต่อไป)

---

## 8. Config (`occupancy.yaml`)

```yaml
cameras:
  - id: cam-1
    name: "Floor 3 - Zone A"
    source: rtsp://user:pass@10.0.0.5:554/stream1   # หรือไฟล์วิดีโอ
    fps: 2
    zones:
      - id: desk-1
        name: "Desk 1"
        roi: [0, 0, 960, 1080]     # x1,y1,x2,y2 ใน source pixels
        away_seconds: 60
      - id: desk-2
        name: "Desk 2"
        roi: [960, 0, 1920, 1080]
        away_seconds: 60

storage:
  log_csv: data/occupancy.csv
  db: data/occupancy.sqlite

dashboard:
  enabled: true
  host: "0.0.0.0"
  port: 8080

pose:
  model: models/pose_landmarker.task
  downscale_width: 480
  min_motion_area: 400
  presence_confidence: 0.5

# optional - ถ้าไม่ใส่ จะใช้ models/yolov8n.pt แล้ว fallback ไปให้ ultralytics
# ดาวน์โหลดเอง และใช้ค่า confidence จาก pose.presence_confidence
detector:
  yolo_model: models/yolov8n.pt
  confidence: 0.5
  torch_threads: 2     # สำคัญ! ดูข้อ 11 - ปล่อยให้ใช้ทุก core กิน CPU เพิ่ม 11 เท่าโดยไม่เร็วขึ้น
```

---

## 9. API endpoints

| Endpoint | คำอธิบาย |
|---|---|
| `GET /` | หน้า dashboard (สถิติ + การ์ดโซน + วิดีโอสด + log) |
| `GET /calibrate` | หน้าตั้งค่าโซนแบบลากเส้น |
| `GET /summary` | JSON สถิติรวม `{total_zones, PRESENT, AWAY}` |
| `GET /status` | JSON สถานะทุกโซน (พร้อม ROI) |
| `GET /events` | JSON event log ล่าสุด |
| `GET /snapshot/{i}` | JPEG snapshot จากกล้อง i (สำหรับหน้า calibrate) |
| `GET /video/{i}` | MJPEG stream พร้อมกรอบ ROI + จุดคนที่ตรวจได้ |
| `POST /save_zones` | รับโซนที่ลาก → apply สด + persist ลงไฟล์ |

---

## 10. ผลการทดสอบความแม่นยำ

### สรุปรวม — ทุกโมเดลที่วัดแล้ว 22 รายการ (อ่านตารางนี้ก่อน)

เรียงจากเร็วไปช้า — นับ variant OpenVINO ของ yolo11n/11s เป็นรายการแยก

ทุกตัววัดด้วยชุดทดสอบเดียวกัน: **46 ภาพจาก COCO val2017 / คน 131 (ถูกจอบัง 69)**

> **วิธีอ่านคอลัมน์ความเร็ว:** ms เทียบข้ามรอบวัด**ไม่ได้** เพราะเป็นโน้ตบุ๊กที่
> throttle ตามความร้อน (yolo11n วัดได้ 110 / 129 / 141 ms ใน 3 รอบ) จึง normalize
> เป็น **"เท่าของ `yolo11s` ในรอบเดียวกัน"** แทน — ค่านี้เทียบข้ามรอบได้
> ส่วน **recall/FP เป็น deterministic** (รัน 2 รอบได้ตรงกันทุกแถว) จึงเทียบได้ตรงๆ

| model | backend | เร็ว (× yolo11s) | รอบ | recall ทุกคน | **recall ถูกบัง** | FP | สรุป |
|---|---|---|---|---|---|---|---|
| **rtmdet-n-person** | onnx+ov | **0.11x** | 2 | 53% | 64% | **0** | เร็วสุด 9 เท่า — โดเมนไม่ตรงชุดทดสอบ ต้องวัดกับกล้องจริง |
| person-detection-retail-0013 | openvino | ~0.21x | เก่า | 43% | 49% | 2 | เทรนกับกล้องร้านค้า — โดเมนไม่ตรง |
| yolo11n + OpenVINO | ov export | ~0.24x | เก่า | 73% | 84% | — | เร็วขึ้น 2.6x จาก pytorch แลกความแม่น 4 จุด |
| person-detection-0202 | openvino | ~0.31x | เก่า | 57% | 61% | 6 | เทรนกับกล้องร้านค้า — โดเมนไม่ตรง |
| **yolo11s + OpenVINO** | ov export | **0.42x** | **3** | 84% | **93%** | 2 | **⭐ เร็วสุดในกลุ่มที่แม่นพอ + FP ต่ำสุด** |
| **dfine-n + OV INT8** | dfine+ov | **0.46x** | **3** | 89% | **97%** | 6 | **⭐ แม่นสุดต่อ ms — แต่ FP สูง 3 เท่า / โมเดลแค่ 5.4 MB** |
| yolo11n | ultralytics | 0.50x | **3** | 76% | 88% | 1 | ค่าเริ่มต้นใน config — เบาสุดที่ยังแม่นพอ (รอบ 2 วัดได้ 0.65x) |
| crowdhuman-yolov8n | ultralytics | 0.52x | 2 | 63% | 75% | 3 | ❌ แย่กว่า yolov8n เปล่าๆ ทุกช่อง |
| **dfine-n + OpenVINO** | dfine+ov | **0.57x** | **3** | **92%** | **97%** | 4 | **⭐ สมดุลดีสุด — เร็วกว่า yolo11s pytorch 1.79x และแม่นกว่า** |
| yolov8n | ultralytics | 0.58x | 2 | 75% | 81% | 1 | ตัวเดิมก่อนเปลี่ยนเป็น yolo11n |
| yolov8s | ultralytics | 0.98x | 2 | 85% | 93% | 2 | yolo11s ดีกว่าเล็กน้อยที่ราคาเท่ากัน |
| **yolo11s** | ultralytics | **1.00x** | — | 85% | **94%** | 2 | **ตัวที่ deploy ปัจจุบัน — baseline ของตาราง** |
| ssdlite320_mobilenet_v3 | torchvision | ~1.02x | เก่า | 68% | 77% | 2 | ❌ ทั้งช้ากว่าและแม่นน้อยกว่า |
| rtmdet-tiny | onnx+ov | 1.22x | 2 | 69% | 83% | 1 | ❌ แพ้ yolo11n ทั้งสองด้าน |
| dfine-n (pytorch) | transformers | 1.68x | **3** | **92%** | **97%** | 4 | ใช้ตัว OpenVINO ข้างบนแทน — เร็วกว่า 2.97x ที่ recall ตรงกันเป๊ะ |
| rtmdet-s | onnx+ov | 1.98x | 2 | 77% | 87% | 1 | ❌ แพ้ yolo11s ทุกช่อง |
| dfine-s | transformers | 4.54x | 2 | 95% | 99% | 6 | แม่นมาก แต่ compute 4.5 เท่า — ยังไม่ได้ลอง OpenVINO |
| rtdetr-l | ultralytics | 4.55x | 2 | 90% | 97% | 2 | dfine-n แทนที่ได้หมด |
| fasterrcnn_mobilenet_v3 | torchvision | ~4.61x | เก่า | 92% | 96% | 5 | ❌ FP เยอะกว่า |
| rtmdet-m-person | onnx+ov | 4.70x | 2 | 85% | 96% | 4 | ไม่คุ้มที่ compute เท่านี้ |
| **dfine-m** | transformers | 8.13x | 2 | **97%** | **100%** | 7 | **แม่นสุด — จับคนถูกบังครบ 69/69** |
| fasterrcnn_resnet50_fpn | torchvision | ~19.5x | เก่า | 97% | 96% | 12 | ❌ ช้าเกินใช้บน edge |

**คอลัมน์ `รอบ`** = รอบที่วัด — แถวที่รอบเดียวกันเทียบกันได้แม่นสุด
**`3`** = 2026-09-16 (`benchmark/results/2026-09-16_1335.json`), **`2`** = 2026-09-15 รอบ `--all`,
**`เก่า`** = รอบก่อนหน้าที่ใช้โปรโตคอลต่างไป (เฟรมคงที่ 480x270) คลาดเคลื่อนสูงสุด

⚠️ **อัตราส่วนเองก็ไม่นิ่ง ±25% ข้ามรอบ** — `yolo11n` วัดได้ 0.50x ในรอบ 3 แต่ 0.65x
ในรอบ 2 เพราะสภาพความร้อนกระทบแต่ละโมเดลไม่เท่ากัน **อย่าอ่านทศนิยมตัวที่สอง
เป็นข้อเท็จจริง** ให้อ่านเป็นลำดับชั้นหยาบๆ แทน ส่วน recall/FP นิ่งจริง (deterministic)

### เลือกตัวไหน

| ถ้าต้องการ | ใช้ | หมายเหตุ |
|---|---|---|
| **false PRESENT ต่ำสุด** ← เกณฑ์ที่สำคัญสุดของงานนี้ | **`yolo11n`** (conf 0.5) | ชุดโต๊ะทำงาน: FP 1 และ **โต๊ะว่างพลาด 0/31** ที่ recall 92% — ตัวที่ตั้งอยู่ใน config แล้ว |
| **แม่นที่สุดต่อ compute** | **`dfine-n` + OpenVINO ที่ `conf=0.6`** | ชุดโต๊ะทำงาน: **recall 100% ทั้งสองช่อง** FP 4 (0.6 กดจาก 9 ได้ฟรี) แต่ยังเด้งโต๊ะว่าง 2/31 |
| **จำนวนกล้องสูงสุด + FP ต่ำ** | **`yolo11s` + OpenVINO** (0.42x) | ชุดเดิม 93% / ชุดโต๊ะทำงาน 100% ที่ conf 0.5 — ขึ้น conf 0.6 ลดโต๊ะว่างพลาดเหลือ 1/31 |
| ประหยัดพื้นที่ edge box | `dfine-n` + OV INT8 (โมเดล 5.4 MB) | เร็วขึ้นแค่ 1.22x จาก FP32 แต่ FP เพิ่มเป็น 6 |
| เบาสุดที่ยังยอมรับได้ | `yolo11n` + OpenVINO | recall ถูกบัง 84% |
| แม่นสุดโดยไม่สนความเร็ว | `dfine-m` (ถูกบัง 100%) | ยังไม่ได้ลอง OpenVINO — น่าจะลงมาเหลือ ~2.7x |
| หลายสิบกล้องต่อ 1 edge box | `rtmdet-n-person` (0.11x) | ชุดโต๊ะทำงาน: **FP 0 / โต๊ะว่างพลาด 0/31** แต่ recall แค่ 76% — ยังต้องวัดกับกล้องจริง |
| ตัดทิ้งได้เลย | `rtmdet-tiny/s`, `crowdhuman-yolov8n`, `ssdlite`, `faster-rcnn`, `rtdetr-l` | |

> ## ⚠️ ข้อจำกัดที่ใหญ่ที่สุดของทุกตัวเลขในหัวข้อนี้ (แก้ไข 2026-09-16 รอบที่ 4)
>
> ตารางสรุปข้างบนวัดด้วย **ชุดเดิม 46 ภาพ** ซึ่งเป็น**ภาพถ่ายระดับสายตาแบบคน
> ถ่ายรูปคน** ไม่ใช่ฉากโต๊ะทำงาน รอบที่ 4 สร้าง **ชุดใหม่ 47 ภาพ (16 positive +
> 31 ภาพโต๊ะว่าง)** ที่บังคับให้เป็นฉากโต๊ะทำงานจริง แล้ว**ผลออกมาต่างกันมาก**:
>
> 1. **recall แทบไม่แยกโมเดลอีกต่อไป** — บนฉากโต๊ะจริงทุกตัวหลักได้ 92-100%
>    (ชุดเดิมกระจาย 76-92%) เพราะคนนั่งหน้าจอเป็นคนตัวใหญ่ในเฟรม
>    **ตัวที่แยกแพ้ชนะคือ false positive** ซึ่งชุดเดิมวัดได้ไม่ครบ
> 2. **อันดับกลับทิศจริงตามที่เตือนไว้** — `dfine-n` ที่ชุดเดิมชี้ว่าดีที่สุด
>    กลับเด้ง PRESENT บนโต๊ะว่าง 3/31 ใบ ขณะที่ `yolo11n` เด้ง 0/31
> 3. ชุดใหม่ก็ **ยังเป็นภาพ COCO** อยู่ดี ไม่ใช่เฟรมจากกล้องที่ติดจริง และ
>    **n เล็กมาก** (คน 25 / โต๊ะว่าง 31) ใช้ชี้ทิศทางได้ ไม่ใช่ตัวเลขรับประกัน
>
> ตัวเลขที่ยัง**เชื่อได้**ทั้งสองชุด: อัตราเร่งจาก OpenVINO/INT8 (2.40-2.97x)
> เพราะเป็นเรื่อง runtime ไม่เกี่ยวกับเนื้อภาพ
>
> **ผลรอบที่ 4 เต็ม อยู่ในหัวข้อย่อย "รอบที่ 4" ด้านล่าง — อ่านก่อนตัดสินใจเรื่องโมเดล**
>
> **หา dataset สาธารณะที่ตรงโจทย์แล้ว: ไม่มี** — สรุปผลค้นในข้อ 14

**บทเรียนที่ใช้ซ้ำได้ 3 ข้อจากตารางนี้:**

1. recall ของ "คนถูกบัง" *สูงกว่า* recall รวมในทุกโมเดล → **การถูกจอบังไม่ใช่จุดอ่อนหลัก**
   เคสที่พลาดจริงคือคนตัวเล็กไกลๆ ภาพเบลอ **การวางกล้องสำคัญกว่าการเลือกโมเดล**
2. อย่าตัดสินสถาปัตยกรรมจากรุ่นใหญ่สุดของมัน — `rtdetr-l` ทำให้สรุปผิดว่า
   "transformer ไม่คุ้ม" ทั้งที่ `dfine-n` ชนะมันทุกด้าน
3. โมเดลที่เทรนกับ **โดเมนอื่น** (กล้องมองลงมา) จะดูแย่บนชุดทดสอบ COCO ระดับสายตา
   เสมอ — ตัวเลขนั้นไม่ใช่หลักฐานว่ามันแย่ ต้องวัดซ้ำด้วยภาพจากกล้องจริง
4. **อย่าเทียบโมเดลข้ามรูปแบบ runtime** — `dfine-n` แพ้ `yolo11s` ตอนเทียบ pytorch
   กับ pytorch (1.71x) แต่ชนะตอนเทียบ OpenVINO กับ pytorch (0.56x vs 1.00x)
   เพราะ OpenVINO เร่ง transformer ได้ดีกว่า CNN (2.97x vs 2.40x)
   **ต้อง export ให้ครบทุกตัวก่อนตัดสิน**

รายละเอียดแต่ละรอบวัดอยู่ในหัวข้อย่อยถัดไป

### เปรียบเทียบโมเดล กับฉาก "คนถูกจอบัง" จริง (2026-09-15)

ชุดทดสอบ: **46 ภาพจาก COCO val2017 / คน 131 คน (ถูกจอบัง 69 คน)** — ground truth
เป็นของ COCO ไม่ใช่การนับเอง รายละเอียดการคัดภาพดู `benchmark/README.md`

| model | ขนาด | ms/เฟรม | recall ทุกคน | **recall คนถูกจอบัง** | false pos |
|---|---|---|---|---|---|
| yolov8n | 6.5 MB | ~55 | 75% | 81% | 1 |
| **yolo11n** ← ค่าเริ่มต้น | **5.6 MB** | **~68** | 76% | **88%** | 1 |
| **yolo11s** ← ตัวเลือกแม่นกว่า | 19.3 MB | **~120** | 85% | **94%** | 2 |
| yolov8s | 22.6 MB | ~120 | 85% | 93% | 2 |

ทั้ง `yolo11n` และ `yolo11s` ลงไว้ใน `models/` แล้ว **สลับได้ที่ `detector.yolo_model`
ใน `occupancy.yaml` บรรทัดเดียว** โดยไม่ต้องดาวน์โหลดอะไรเพิ่ม

| | yolo11n | yolo11s |
|---|---|---|
| recall คนถูกจอบัง | 88% (พลาด 12%) | 94% (พลาด 6%) |
| ms/เฟรม | ~68 | ~120 |
| edge 4-core @ 2fps | ~12-15 กล้อง | ~6-8 กล้อง |

**ข้อสังเกตสำคัญ:** recall ของ "คนถูกจอบัง" (88-94%) *สูงกว่า* recall รวมทุกคน (76-85%)
แปลว่า **การถูกจอบังไม่ใช่จุดอ่อนหลัก** — เคสที่พลาดจริงคือคนตัวเล็กไกลๆ ภาพเบลอ
และคนที่เห็นแค่แขน/มือในฝูงชน ดังนั้น **การวางกล้องให้คนอยู่ใหญ่พอในเฟรม
สำคัญกว่าการเลือกโมเดล**

> ข้อจำกัด: COCO เป็นภาพระดับสายตา ไม่ใช่มุม CCTV มองลงมา
> ลำดับอาจเปลี่ยนเมื่อติดกล้องจริง — รัน `benchmark/benchmark_models.py` ซ้ำด้วยภาพจากกล้องตัวเองได้

### โมเดลนอกตระกูล YOLO ที่ลองแล้ว

วัดด้วยชุดทดสอบเดียวกัน (46 ภาพ / คน 131 / ถูกจอบัง 69):

| model | ms | recall ทุกคน | recall ถูกบัง | FP | สรุป |
|---|---|---|---|---|---|
| yolo11n | ~110 | 76% | 88% | 1 | baseline |
| yolo11s | ~176 | 85% | 94% | 2 | baseline |
| **rtdetr-l** (transformer) | ~964 | 90% | **97%** | 2 | แม่นสุด แต่ช้ากว่า yolo11s 5 เท่า |
| fasterrcnn_mobilenet_v3 | ~812 | 92% | 96% | 5 | ช้ากว่า yolo11s 4 เท่า FP เยอะกว่า |
| fasterrcnn_resnet50_fpn | ~3432 | **97%** | 96% | 12 | ช้าเกินใช้บน edge |
| ssdlite320_mobilenet_v3 | ~179 | 68% | 77% | 2 | ทั้งช้ากว่าและแม่นน้อยกว่า yolo11s |

**ไม่มีตัวไหนชนะ `yolo11s` ในแง่ speed/accuracy** — `rtdetr-l` แม่นกว่าจริง (97% vs 94%)
แต่ต้องจ่าย compute 5 เท่า ซึ่งบน edge box ไม่คุ้ม

> ⚠️ **ข้อสรุปนี้ถูกแก้ในรอบถัดไป** — "transformer ไม่คุ้ม" มาจากการวัด `rtdetr-l`
> ซึ่งเป็นรุ่น **ใหญ่สุด** ของตระกูล รุ่นเล็กกว่าให้ผลต่างออกไปมาก ดูหัวข้อถัดไป

### รอบที่ 2 — D-FINE / RTMDet / CrowdHuman (2026-09-15)

ชุดทดสอบเดียวกัน แต่รันใหม่ทั้งตารางใน session เดียว **จำกัด 2 threads เท่ากันหมด**
และจับเวลาครอบ preprocess+inference+postprocess ทั้งก้อน (ms จึงสูงกว่าตารางข้างบน
ทุกตัว — เทียบข้ามตารางไม่ได้ ให้เทียบภายในตารางเดียวกันเท่านั้น)

| model | backend | ms | recall ทุกคน | recall ถูกบัง | FP |
|---|---|---|---|---|---|
| yolo11n | ultralytics | 140.6 | 76% | 88% | 1 |
| yolo11s ← ใช้จริง | ultralytics | 217.3 | 85% | 94% | 2 |
| **dfine-n** | transformers | 433.9 | **92%** | **97%** | 4 |
| **dfine-s** | transformers | 987.2 | 95% | **99%** | 6 |
| **dfine-m** | transformers | 1767.5 | **97%** | **100%** | 7 |
| rtdetr-l | ultralytics | 989.3 | 90% | 97% | 2 |
| rtmdet-tiny | onnx+openvino | 265.0 | 69% | 83% | 1 |
| rtmdet-s | onnx+openvino | 431.1 | 77% | 87% | 1 |
| **rtmdet-n-person** | onnx+openvino | **23.2** | 53% | 64% | **0** |
| rtmdet-m-person | onnx+openvino | 1021.1 | 85% | 96% | 4 |
| crowdhuman-yolov8n | ultralytics | 113.3 | 63% | 75% | 3 |

**สิ่งที่ได้จากรอบนี้ 4 ข้อ:**

1. **D-FINE ล้ม `rtdetr-l` ขาด** — `dfine-n` แม่นเท่า `rtdetr-l` (ถูกบัง 97% ทั้งคู่)
   แต่**เร็วกว่า 2.3 เท่า** และ recall รวมดีกว่า (92% vs 90%) ข้อสรุปเดิมที่ว่า
   transformer detector ไม่คุ้มจึงผิด — มันเป็นข้อจำกัดของ *รุ่น* ไม่ใช่ *สถาปัตยกรรม*
   `dfine-m` จับคนถูกบังได้ครบ **69/69 = 100%**

2. **แต่ยังไม่เปลี่ยน production** — `dfine-n` (434 ms) ยังช้ากว่า `yolo11s` (217 ms)
   สองเท่า และเป็น PyTorch ล้วน **ยังไม่ได้ export OpenVINO** ถ้าได้อัตราเร่งเท่าที่
   YOLO ได้ (1.7-2.6 เท่า) จะลงมา ~170-255 ms ที่ recall ถูกบัง 97%
   = **ชนะ `yolo11s` ทั้งสองด้าน** → เป็นการทดลองถัดไปที่คุ้มสุด (ข้อ 14)
   ข้อควรระวัง: FP ของ D-FINE สูงกว่า (4-7 vs 2) ซึ่งแปลว่า "PRESENT ตอนไม่มีคน"
   ต้องจูน `conf` ขึ้นก่อนใช้จริง

3. **RTMDet แพ้ขาด ตัดทิ้งได้** — `rtmdet-tiny` (265 ms/83%) ทั้งช้ากว่าและแม่นน้อยกว่า
   `yolo11n` (141 ms/88%) ส่วน `rtmdet-s` แพ้ `yolo11s` ทุกช่อง
   แต่ `rtmdet-n-person` ที่ **23.2 ms (เร็วกว่า `yolo11s` 9.4 เท่า) FP = 0**
   เข้าข่ายเดียวกับ OpenVINO person-detection ข้างล่าง — เทรนกับกล้องมองลงมา
   แต่ถูกทดสอบด้วยภาพ COCO ระดับสายตา **ต้องวัดซ้ำกับกล้องจริงก่อนตัดสิน**
   ที่ 23 ms มันเป็นตัวเดียวที่รันได้หลายสิบกล้องบน edge box ตัวเดียว

4. **CrowdHuman fine-tune ล้มเหลว — สมมติฐานผิด** คาดว่าน้ำหนักที่เทรนกับฉาก
   แออัด/ถูกบังจะชนะเรื่องคนถูกบัง ผลออกมาตรงข้าม (63%/75%) **แย่กว่า `yolov8n`
   เปล่าๆ ทุกช่อง** สาเหตุที่น่าจะเป็น: checkpoint เป็น `best.pt` จาก training run
   ของคนทั่วไปบน HF ไม่มีตัวเลขยืนยัน + เทรนคลาสเดียวบน dataset แคบจึงเสีย
   generality + **นิยามกล่องต่างกัน** (CrowdHuman ใช้ full-body รวมส่วนที่ถูกบัง
   COCO ใช้เฉพาะส่วนที่มองเห็น) ทำให้ center เลื่อนจาก GT
   ถ้าจะเอาแนวคิดนี้จริงต้อง fine-tune เองจาก dataset ต้นฉบับ ไม่ใช่หยิบจาก hub

รันตารางนี้ซ้ำสองรอบ **recall กับ FP ออกมาตรงกันทุกแถว** (deterministic)
ส่วน ms แกว่ง 3-20% ตามความร้อนของโน้ตบุ๊ก — ยืนยันอีกครั้งว่าให้เชื่อ
อัตราส่วนภายในรันเดียวกัน ไม่ใช่ค่าสัมบูรณ์

รายละเอียดคำสั่งที่รันและวิธีอ่านผล: `benchmark/example_run.md`
ผลดิบพร้อม `_meta` (วันที่/argv/threads/conf): `benchmark/results_latest.json`

### รอบที่ 3 — D-FINE + OpenVINO (2026-09-16) ⭐ รอบที่เปลี่ยนข้อสรุป

ทดลองตามที่ค้างไว้จากรอบที่ 2: export `dfine-n` เป็น OpenVINO IR แล้ววัดซ้ำ
ใช้ `benchmark/export_dfine_openvino.py` (FP32 + INT8 ผ่าน NNCF)
**pre/postprocess ใช้ processor ตัวเดิมทุกบรรทัด ต่างกันแค่ runtime**

| model | ms | × yolo11s | recall ทุกคน | **recall ถูกบัง** | FP | ขนาด |
|---|---|---|---|---|---|---|
| yolo11n (pytorch) | 48.2 | 0.50x | 76% | 88% | 1 | 5.6 MB |
| yolo11s (pytorch) | 97.0 | 1.00x | 85% | 94% | 2 | 19.3 MB |
| yolo11s + OpenVINO | **40.4** | **0.42x** | 84% | 93% | 2 | ~19 MB |
| dfine-n (pytorch) | 163.2 | 1.68x | 92% | 97% | 4 | — |
| **dfine-n + OpenVINO** | **55.0** | **0.57x** | **92%** | **97%** | 4 | 15.9 MB |
| dfine-n + OV INT8 | **45.0** | **0.46x** | 89% | **97%** | 6 | **5.4 MB** |

> ⚠️ ms ของรอบนี้ต่ำกว่ารอบที่ 2 **ราว 2.2 เท่าทุกแถว** (yolo11s 97.0 vs 217.3)
> เพราะเครื่องเย็นกว่า — ไม่ใช่ว่าอะไรเร็วขึ้น ให้ดูคอลัมน์ `× yolo11s` แทน

**1. สมมติฐานถูก — `dfine-n` + OpenVINO ชนะ `yolo11s` ทั้งสองด้าน**

OpenVINO เร่ง `dfine-n` ได้ **2.97 เท่า** (163.2 → 55.0 ms) ซึ่ง**มากกว่าที่ YOLO ได้**
(yolo11s ได้ 2.40 เท่า) ผลคือ:

| เทียบกับ | ความเร็ว | recall ทุกคน | recall ถูกบัง |
|---|---|---|---|
| `yolo11s` pytorch (baseline เดิม) | **เร็วกว่า 1.76 เท่า** | **+7 จุด** | **+3 จุด** |
| `yolo11s` + OpenVINO | ช้ากว่า 1.36 เท่า | **+8 จุด** | **+4 จุด** |

**recall ออกมาตรงกับ PyTorch เป๊ะทุกตัวเลข** (120/131, 67/69, FP 4) ยืนยันว่าการ
แปลงไม่ทำให้ผลเปลี่ยน — ต่างจาก YOLO ที่ export แล้วความแม่นตกเล็กน้อย (94% → 93%)

**2. INT8 ได้ผลน้อยกว่าที่คาด และมีราคา**

เร่งได้แค่ **1.22 เท่า** (55.0 → 45.0 ms) ไม่ใช่ 2-3 เท่าตามที่คาดไว้ในข้อ 14 เดิม
เพราะต้องส่ง `model_type=TRANSFORMER` ให้ NNCF ซึ่งกัน attention/softmax ไม่ให้
quantize (ถ้าไม่กัน ความแม่นตกหนัก) จึงเหลือส่วนที่เป็น INT8 จริงน้อยกว่า CNN

ราคาที่จ่าย: recall รวมตก 92% → 89% และ **FP เพิ่ม 4 → 6** ส่วน recall คนถูกบัง
ยังอยู่ 97% เท่าเดิม กำไรที่ได้จริงคือ**ขนาดโมเดล 15.9 → 5.4 MB (เล็กลง 2.9 เท่า)**
ซึ่งสำคัญกับ edge box ที่พื้นที่จำกัด แต่ไม่ใช่การเร่งความเร็ว

**3. การตัดสินใจเปลี่ยนเป็น 3 ทางเลือก ไม่ใช่ทางเดียว**

| ตัวเลือก | ms | ถูกบัง | FP | เหมาะกับ |
|---|---|---|---|---|
| `yolo11s` + OpenVINO | 40.4 | 93% | 2 | จำนวนกล้องสูงสุด / FP ต่ำสุด |
| **`dfine-n` + OV INT8** | 45.0 | **97%** | 6 | **แม่นสุดต่อ ms — แต่ FP สูง 3 เท่า** |
| **`dfine-n` + OpenVINO FP32** | 55.0 | **97%** | 4 | **สมดุลที่ดีที่สุดถ้ารับ 1.36x ได้** |

FP สำคัญกับงานนี้เป็นพิเศษ เพราะแปลเป็น **"รายงาน PRESENT ตอนไม่มีคน"**
ซึ่งผิดในทิศที่แย่กว่าการพลาดคน — ก่อนสลับไป D-FINE **ต้องจูน `conf` ขึ้นจาก 0.5
แล้ววัด FP ซ้ำ** (ยังไม่ได้ทำ) ถ้าจูนแล้ว FP ลงมาระดับ 2 โดย recall ยังสูง
`dfine-n` + OpenVINO จะเป็นตัวที่ควร deploy แทน `yolo11s`

**ยังไม่เปลี่ยน `occupancy.yaml`** เพราะ (ก) ยังไม่จูน conf, (ข) `desk_occupancy_multi.py`
รองรับแต่โมเดลที่โหลดผ่าน `ultralytics` ยังต้องเพิ่มโค้ดฝั่ง production รองรับ D-FINE

### รอบที่ 4 — ชุดทดสอบ "คนนั่งหน้าจอ" + ภาพโต๊ะว่าง (2026-09-16) ⭐ รอบที่พลิกเกณฑ์ตัดสิน

รอบที่ 3 ฝากปัญหาไว้ 2 ข้อ — รอบนี้แก้ทั้งคู่ด้วย `benchmark/build_desk_testset.py`

1. ชุดทดสอบเป็น**ภาพถ่ายคน**ระดับสายตา ไม่ใช่**ฉากโต๊ะทำงาน**
2. ไม่มีภาพ "ไม่มีคน" เลย จึงวัด **false PRESENT** ซึ่งเป็น failure mode ที่แย่ที่สุด
   ของระบบนี้ไม่ได้ตรงๆ (FP เดิมนับแค่ "detection ที่ไม่ตกในกล่องคน")

**ชุดใหม่ `desk_testset.json` — 47 ภาพ** (ยังมาจาก COCO val2017 / GT เป็นของ COCO)

| ส่วน | จำนวน | เกณฑ์ |
|---|---|---|
| positive | 16 ภาพ / คน 25 (ถูกอุปกรณ์บัง 17) | คนนั่งที่โต๊ะซึ่งมี `laptop`/`keyboard` **อยู่ตรงหน้า** ยืนยันท่านั่งด้วย COCO keypoints |
| negative | 31 ภาพ / **คน 0** | ภาพที่มีเก้าอี้ + อุปกรณ์ทำงาน แต่ **ไม่มี `person` เลย** = โต๊ะว่าง |

เกณฑ์คัดผ่านการ**ตรวจด้วยตา 2 รอบ** — รอบแรกได้มอเตอร์ไซค์ นักเทนนิสเสิร์ฟ
กรรมการบาส ม้านั่งสวน ห้องน้ำ หลุดเข้ามาเยอะ (ขายกทำให้ระยะสะโพก-เข่าสั้น
เหมือนท่านั่ง) จึงเพิ่มเงื่อนไข "ต้นขาต้องชี้ไปข้างหน้า" + ตัดหมวดกลางแจ้ง/กีฬา
+ บังคับว่าอุปกรณ์ต้องซ้อนกล่องคนและอยู่ในช่วง x ของเขา รายละเอียดดู
`benchmark/README.md` และ docstring ของ `build_desk_testset.py`

**ผลวัด** (`benchmark/results/2026-09-16_1506.json`, conf=0.5, 2 threads)

| model | ms | recall ทุกคน | recall ถูกบัง | FP | **โต๊ะว่าง → PRESENT** |
|---|---|---|---|---|---|
| rtmdet-n-person | **10.3** | 76% (19/25) | 76% (13/17) | **0** | **0/31** |
| models/yolo11s_openvino_model | 42.0 | **96%** (24/25) | **100%** (17/17) | 3 | 2/31 |
| crowdhuman-yolov8n | 42.9 | 84% | 88% | 14 | 3/31 |
| dfine-n-ov-int8 | 46.9 | **100%** | **100%** | 13 | 3/31 |
| yolo11n.pt | 49.5 | 92% (23/25) | 94% (16/17) | **1** | **0/31** |
| dfine-n-ov | 60.2 | **100%** (25/25) | **100%** (17/17) | 9 | 3/31 |
| yolo11s.pt | 95.0 | 92% | 94% | 5 | 1/31 |
| rtmdet-tiny | 124.2 | 84% | 88% | 2 | 2/31 |
| dfine-n (pytorch) | 184.2 | **100%** | **100%** | 9 | 3/31 |

**1. recall ไม่ใช่ตัวแยกอีกต่อไป — FP คือตัวแยก**

บนฉากโต๊ะทำงานจริง ทุกโมเดลหลัก recall ขึ้นมาอยู่ที่ 92-100% (ชุดเดิม 76-92%)
เพราะคนนั่งหน้าจอเป็น**คนตัวใหญ่ในเฟรม** ไม่ใช่คนตัวจิ๋วไกลๆ ที่ทำให้ recall
ชุดเดิมตก ความต่างระหว่าง `yolo11n` กับ `dfine-n` เหลือแค่ **2 คนจาก 25** —
เล็กกว่าที่ชุดเดิมบอกมาก ในขณะที่ **FP ต่างกัน 9 เท่า (1 vs 9)**

**2. D-FINE จ่ายค่าความแม่นด้วย false PRESENT — ตรงจุดที่งานนี้แพ้ไม่ได้**

`dfine-n` เด้ง PRESENT บนโต๊ะว่าง **3 ใน 31 ใบ** ส่วน `yolo11n` เด้ง **0 ใบ**
ไล่ดูรายภาพว่าเด้งที่ไหน:

| ภาพ | ของจริงในภาพ | ใครเด้ง |
|---|---|---|
| `000000246308` | โต๊ะที่มี**ชั้นวางฟิกเกอร์หุ่น** (Ninja Turtles / Gundam / Star Wars) | **ทุกตัว** — yolo11s เจอ 3, dfine 5, crowdhuman 10 |
| `000000368684` | ห้องนั่งเล่น มี**คนอยู่บนจอทีวี** | dfine-n, (yolo11s-ov ไม่เด้ง) |
| `000000571313` | ห้องรก มีเสื้อคลุมพาดเก้าอี้ | dfine-n, rtmdet-tiny |
| `000000372466` | โคลสอัพเมาส์/แล็ปท็อป | yolo11s-ov |
| `000000078420` | **แมวนอนข้างแล็ปท็อป** | crowdhuman-yolov8n |

สองเคสแรกคือเคสที่จะเจอจริงในออฟฟิศ: **ฟิกเกอร์/โปสเตอร์คนบนโต๊ะ** และ
**คนบนหน้าจอ** (video call / วิดีโอที่เปิดค้าง) ทั้งคู่แปลเป็น "รายงานว่ามีคน
นั่งอยู่ทั้งที่โต๊ะว่าง" ซึ่งทำลายความน่าเชื่อถือของรายงาน occupancy ทั้งชุด

**3. จูน `conf` แล้ว — งานที่ค้างจากรอบที่ 3 (ทำเสร็จแล้ว)**

| model | conf | recall ทุกคน | ถูกบัง | FP | โต๊ะว่าง → PRESENT |
|---|---|---|---|---|---|
| yolo11n | **0.5** | **92%** (23/25) | 16/17 | 1 | **0/31** |
| yolo11n | 0.6 | 84% | 14/17 | 1 | 0/31 |
| yolo11n | 0.7 | 80% | 14/17 | 0 | 0/31 |
| yolo11s + OV | 0.5 | 96% | 17/17 | 3 | 2/31 |
| yolo11s + OV | **0.6** | 92% | 16/17 | **2** | **1/31** |
| yolo11s + OV | 0.7 | 88% | 16/17 | 2 | 1/31 |
| dfine-n-ov | 0.5 | 100% | 17/17 | 9 | 3/31 |
| **dfine-n-ov** | **0.6** | **100%** (25/25) | **17/17** | **4** | **2/31** |
| dfine-n-ov | 0.7 | 88% | 15/17 | 1 | 1/31 |
| dfine-n-ov | 0.8 | 76% | 13/17 | 0 | 0/31 |

**`dfine-n-ov` ที่ `conf=0.6` คือของฟรี** — recall คงที่ 100% ทั้งสองช่องเป๊ะ
แต่ FP ลดครึ่ง (9 → 4) และโต๊ะว่างพลาดลดจาก 3 → 2 ใบ ใครใช้ D-FINE ควรตั้ง 0.6
ไม่ใช่ 0.5 เลย ส่วน **0.7 ขึ้นไปไม่คุ้ม** — recall ร่วงเป็นหน้าผา (100% → 88% → 76%)

`yolo11n` ตรงข้าม: **0.5 คือจุดที่ดีที่สุดอยู่แล้ว** ขึ้น conf ได้แต่เสีย recall
เพราะ FP มันต่ำสุดตั้งแต่แรก (1 และ 0/31) — ไม่มีอะไรให้กด

**4. `rtmdet-n-person` ยังคงเป็นตัวที่น่าจับตา**

เร็วสุด 10.3 ms (**4.8 เท่าของ `yolo11n`**) และ **FP = 0 / โต๊ะว่างพลาด 0/31**
แลกกับ recall 76% ซึ่งบนชุดนี้แปลว่าพลาด 6 คนจาก 25 — ยังเป็นตัวเดียวที่
โดเมนการเทรน (กล้องมองลงมา) ตรงกับที่จะติดจริงมากกว่า COCO

> ### ⚠️ อย่าอ่านตารางรอบนี้แรงเกินหลักฐาน
>
> **n เล็กมาก** — คน 25 กับโต๊ะว่าง 31 ใบ ความต่าง "92% vs 100%" คือ **2 คน**
> และ "0/31 vs 3/31" คือ **3 ภาพ** ระดับความเชื่อมั่นจึงหยาบ ใช้ตัดสินได้แค่ว่า
> **ทิศทางไหนน่าเสี่ยงกว่า** ไม่ใช่ตัวเลขที่จะเอาไปรับประกันกับลูกค้า
>
> **ยังเป็นภาพ COCO อยู่ดี** — คัดให้เป็นฉากโต๊ะทำงานได้ แต่ยังเป็นภาพถ่าย
> ที่คนถ่าย ไม่ใช่เฟรมจากกล้องที่ติดเพดานมองลงมา เคส **แสงจากจอในห้องมืด**
> และ **คนเดินผ่านที่ไม่ได้นั่ง** ยังไม่มีในชุดนี้

**ยังไม่เปลี่ยน `occupancy.yaml`** — `yolo11n` + conf 0.5 (ค่าปัจจุบัน) ออกมาเป็น
ตัวที่ปลอดภัยที่สุดเรื่อง false PRESENT บนชุดนี้อยู่แล้ว การสลับไป D-FINE
ยังต้องเขียนโค้ดฝั่ง production เพิ่ม และแลก FP สูงกว่า 4 เท่าเพื่อ recall +8 จุด
ซึ่ง**บนชุด n=25 ยังไม่ใช่หลักฐานพอ**

### โมเดลที่เทรนกับภาพ CCTV โดยเฉพาะ (OpenVINO Model Zoo)

| model | ms | recall ทุกคน | recall ถูกบัง | FP |
|---|---|---|---|---|
| person-detection-retail-0013 | **37.7** | 43% | 49% | 2 |
| person-detection-0202 | 55.5 | 57% | 61% | 6 |

> **อย่าเพิ่งตัดทิ้งจากตัวเลขนี้** — สองตัวนี้เทรนกับภาพกล้องมองลงมาในร้านค้า
> แต่ชุดทดสอบเป็นภาพ COCO ระดับสายตา = **คนละโดเมนกับที่มันเทรนมา**
> การวัดนี้จึงลำเอียงต่อต้านมัน ไม่ใช่หลักฐานว่ามันแย่
>
> จุดที่น่าสนใจ: มันเร็วกว่า yolo11n ~3 เท่า ถ้ากล้องจริงเป็นมุมมองลงมา
> ผลอาจกลับทิศ — ควรวัดซ้ำด้วยภาพจากกล้องที่ติดจริงก่อนตัดสิน

### OpenVINO export — ของฟรีที่ควรใช้

export โมเดล YOLO ตัวเดิมเป็น OpenVINO แล้ววัดใหม่ (เฟรมขนาดคงที่ 480x270):

| model | ms | เทียบ | recall ทุกคน | recall ถูกบัง |
|---|---|---|---|---|
| yolo11n (pytorch) | 108.4 | — | 76% | 88% |
| yolo11n (openvino) | **42.2** | **เร็วขึ้น 2.6x** | 73% | 84% |
| yolo11s (pytorch) | 176.2 | — | 85% | 94% |
| **yolo11s (openvino)** | **103.7** | **เร็วขึ้น 1.7x** | 84% | **93%** |

**ผลสำคัญ: `yolo11s` + OpenVINO (103.7 ms, ถูกบัง 93%) เร็วกว่า `yolo11n` แบบ PyTorch
(108.4 ms, ถูกบัง 88%) และแม่นกว่าด้วย** — ได้ทั้งสองอย่างโดยไม่ต้องแลก

วิธี export:

```bash
python -c "from ultralytics import YOLO; YOLO('models/yolo11s.pt').export(format='openvino', imgsz=480)"
```

แล้วชี้ `detector.yolo_model` ไปที่โฟลเดอร์ `models/yolo11s_openvino_model` ได้เลย
(โค้ดรองรับแล้ว — ถ้า path เป็นโฟลเดอร์จะโหลดแบบ OpenVINO อัตโนมัติ)

> ตัวเลข ms บนเครื่องนี้ (Ryzen 5 7530U โน้ตบุ๊ก) แกว่งตามความร้อน/โหลด
> **อัตราส่วนภายในการรันเดียวกันเชื่อถือได้กว่าค่าสัมบูรณ์**

### รอบล่าสุด — รันจริงเต็ม pipeline (2026-09-15, Windows 11 / 12-core CPU)

ทดสอบกับ `data/sample_real.mp4` (สังเคราะห์จากภาพคนจริง 960x540, วนลูป 20 วิ)
โซนซ้ายจงใจวาง**จอคอมพิวเตอร์ไว้แต่ไม่มีคน** เพื่อจับ false positive แบบที่ HOG เคยพลาด

| โซน | ความเป็นจริง | ผลที่ได้ |
|---|---|---|
| desk-1 (ซ้าย — จอคอม ไม่มีคน) | ไม่มีคน | `AWAY`, `person_count=0` **ตลอด 60 วินาที** ✓ |
| desk-2 (ขวา — มีคนเป็นช่วง) | มีคน 7 วิ / ว่าง 13 วิ | `AWAY` → `PRESENT` → `AWAY` วนถูกตามจังหวะ ✓ |

- **false positive = 0** ไม่มี event ของ desk-1 เกิดขึ้นเลยแม้แต่ครั้งเดียว
- จุดเหลือง (person center) ตกบนลำตัวคนพอดี กรอบเขียว/แดงตรงตามสถานะ
- สถานะ `(PRESENT, person_count=0)` ที่เห็นช่วงสั้นๆ คือช่วง grace ของ `away_seconds` — ถูกต้องตาม design

> วิดีโอทดสอบเป็นภาพสังเคราะห์ ยัง**ไม่ได้**ทดสอบกับกล้อง CCTV จริงในออฟฟิศ
> ตัวเลขความแม่นยำกับมุมกล้องจริง/แสงจริงยังต้องวัดซ้ำหลังติดตั้งกล้อง

### รอบก่อน (session claude.ai) — ยังไม่ได้ verify ซ้ำ

ระบุว่าทดสอบกับวิดีโอจริงจาก Mixkit แล้วถูกต้องทั้ง 4 โซน (desk-1 `AWAY`, desk-2/3/4 `PRESENT`)
เป็นคนละโค้ดกับเวอร์ชันปัจจุบัน

---

## 11. การใช้ทรัพยากร

วัดจริงบน Windows 11 / 12-core CPU (ไม่ใช้ GPU) — 1 กล้อง 2 โซน @ 2 fps, YOLOv8n @ 480px:

| ตัวชี้วัด | ค่า |
|---|---|
| inference | **~68 ms** (yolo11n) / **~120 ms** (yolo11s) |
| CPU | **29% ของ 1 core** (2.4% ของเครื่อง 12 core) |
| RAM | **~410 MB** (torch กินเป็นหลัก) |
| ดิสก์ | venv + torch ~**1.1 GB**, โมเดล 6.5 MB |

### ⚠️ กับดัก OpenMP busy-wait (เจอตอนวัดจริง)

ตอนแรกวัดได้ CPU **334% ของ 1 core** ทั้งที่ inference ใช้จริงแค่ ~9% (44ms × 2 เฟรม/วิ)
สาเหตุคือ OpenMP ของ torch ดีฟอลต์เป็น **busy-wait** — thread ที่ทำงานเสร็จจะวนรอเปล่าๆ
แทนที่จะหลับ ทำให้ CPU พุ่งทั้งที่ระบบแทบไม่ทำอะไร

แก้ในโค้ดแล้ว 2 จุด:

```python
os.environ.setdefault("OMP_WAIT_POLICY", "PASSIVE")   # ต้องตั้งก่อน import torch
torch.set_num_threads(2)                              # ปรับผ่าน detector.torch_threads
```

ผลวัดซ้ำหลังแก้: **334% → 29%** (ลดลง 11 เท่า)

เหตุผลที่จำกัดแค่ 2 threads ได้โดยไม่เสียความเร็ว — วัดแล้วเวลาต่อเฟรมแทบไม่ต่างกัน:

| torch threads | ms/เฟรม |
|---|---|
| 6 (default = ครึ่งหนึ่งของ core) | 43.8 |
| 4 | 48.5 |
| 2 (**ค่าที่ใช้**) | 47.1 |
| 1 | 51.4 |

### ประมาณการขนาดเครื่อง

| โมเดล | ms/เฟรม | ต่อกล้องที่ 2 fps | edge 4-core |
|---|---|---|---|
| `yolo11n` | ~68 | ~13.6% ของ 1 core | ~12-15 กล้อง |
| `yolo11s` | ~120 | ~24% ของ 1 core | ~6-8 กล้อง |

(จำนวนโซนต่อกล้องแทบไม่เพิ่มภาระ เพราะตรวจเต็มเฟรมครั้งเดียวแล้วค่อยแมปเข้า ROI) (จำนวนโซนต่อกล้องแทบไม่เพิ่มภาระ
เพราะตรวจเต็มเฟรมครั้งเดียวแล้วค่อยแมปเข้า ROI)

---

## 12. ข้อจำกัดที่ต้องรู้

- **"ไม่ขยับ" ≠ "ไม่ทำงาน"** ระบบวัด occupancy เท่านั้น ไม่ใช่ประสิทธิภาพ
- **แม่นยำขึ้นเมื่อ ROI แม่นยำ** ใช้หน้า calibrate ลากกรอบให้ตรงบริเวณตัวคน/เก้าอี้ ห้ามกว้างเกินไป
- **คนเดินผ่าน** อาจทำให้โซนเป็น `PRESENT` ชั่วคราว (ระบบรอ `away_seconds` ก่อนเปลี่ยน `AWAY`)
- **แสงน้อย/มุมบัง** อาจลดความแม่นยำ — ตั้งกล้องให้มองเห็นชัด
- **OpenCV 5.x ตัด `cv2.HOGDescriptor` ออกแล้ว** fallback ชั้นสุดท้ายจึงใช้ไม่ได้บน cv2 5.x
  ถ้าไม่มีทั้ง `ultralytics` และไฟล์ MediaPipe โปรแกรมจะหยุดพร้อมบอกวิธีแก้
  (แนะนำให้ติดตั้ง `ultralytics` อยู่แล้วเพราะแม่นสุด)
- **ชื่อโซนภาษาไทยบนวิดีโอ** ฟอนต์ Hershey ของ OpenCV วาดไทยไม่ได้ (ออกมาเป็น `????`)
  โค้ดจึงใช้ Pillow + ฟอนต์ไทยในเครื่อง (Leelawadee UI / Tahoma / Loma) วาดแทน
  ถ้าหาฟอนต์ไม่เจอจะถอยไปแสดง **zone id** (ASCII) แทนชื่อ — หน้า dashboard แสดงไทยได้ปกติเสมอ
- **อ่านไฟล์วิดีโอจะช้ากว่าเวลาจริง** worker อ่านเฟรมตาม `fps` ที่ตั้ง (เช่น 2) แต่ไฟล์อาจเป็น 10 fps
  เวลาทดสอบกับไฟล์ให้ตั้ง `fps` ให้ตรงกับไฟล์ ถ้าอยากให้เล่นเป็นเวลาจริง (กับ RTSP ไม่มีปัญหานี้)

---

## 13. ข้อควรระวังด้านกฎหมาย (PDPA ประเทศไทย)

- **ฐานทางกฎหมาย** ต้องมีเหตุผลที่ชอบด้วยกฎหมาย ปรึกษา HR/ฝ่ายกฎหมายก่อนนำไปใช้
- **แจ้งพนักงานล่วงหน้า** ทำนโยบายเขียนชัดเจนว่าเก็บอะไร เพื่ออะไร ใครเข้าถึง นานเท่าไร
- **เก็บให้น้อยที่สุด** วัดสถานะ occupancy ไม่ใช่จับใบหน้า/ระบุตัวบุคคล ไม่เก็บวิดีโอดิบ
- **ความปลอดภัยข้อมูล** เก็บ log ในเครือข่ายองค์กร จำกัดสิทธิ์ กำหนดอายุเก็บข้อมูล
- **ใช้เพื่อปรับปรุงงาน** ไม่ใช่ลงโทษอย่างเดียว

> **หมายเหตุ:** ส่วนนี้เป็นแนวทางทั่วไป ไม่ใช่คำแนะนำทางกฎหมาย
> ให้ฝ่ายกฎหมายขององค์กรตรวจสอบก่อนใช้งานจริง

---

## 14. แนวทางต่อยอด (ยังไม่ได้ทำ)

- ส่งแจ้งเตือนเข้า Line/Slack webhook เมื่อโซนเป็น `AWAY`
- รายงานสรุปรายวัน (เปอร์เซ็นต์ occupancy ต่อโซน/ต่อช่วงเวลา)
- export รายงานเป็น PDF/Excel
- รองรับ polygon ROI (ไม่ใช่แค่กรอบสี่เหลี่ยม)
- **export โมเดลเป็น OpenVINO / ONNX** — บน edge box ที่เป็น CPU Intel มักเร็วขึ้น 2-3 เท่า
  (`YOLO("yolo11n.pt").export(format="openvino")`) ยังไม่ได้ลองวัดในโปรเจกต์นี้
- ~~**⭐⭐ เปลี่ยนชุดทดสอบให้ตรงโจทย์**~~ **ทำแล้วบางส่วน (รอบที่ 4, 2026-09-16)**
  สร้าง `benchmark/desk_testset.json` — 16 ภาพคนนั่งที่โต๊ะ + **31 ภาพโต๊ะว่าง**
  ซึ่งเพิ่มเคส "เก้าอี้ว่างที่ไม่ควรนับเป็นคน" ที่ชุดเดิมไม่มีเลย ผลพลิกอันดับจริง
  (ดูข้อ 10 รอบที่ 4) **ที่ยังขาด:** ยังเป็นภาพ COCO ไม่ใช่เฟรมจากกล้องที่ติดจริง
  และยังไม่มีเคส **คนเดินผ่านที่ไม่ได้นั่ง** กับ **แสงจากจอในห้องมืด**
- ~~**⭐ จูน `conf` ของ `dfine-n`**~~ **ทำแล้ว (รอบที่ 4)** — `conf=0.6` คือของฟรี:
  recall คงที่ 100% ทั้งสองช่องแต่ FP ลดครึ่ง (9 → 4) ส่วน 0.7 ขึ้นไปไม่คุ้ม
  recall ร่วงเป็นหน้าผา **แต่ยังไม่พอให้สลับ deploy** เพราะที่ conf 0.6 ยังเด้ง
  PRESENT บนโต๊ะว่าง 2/31 ใบ ขณะที่ `yolo11n` เด้ง 0/31
- **⭐⭐ ชุดวัดจากกล้องของตัวเอง — ข้อที่เหลืออยู่และสำคัญที่สุด**
  ค้น dataset สาธารณะแบบ deep-research แล้ว (2026-09-16) **ข้อสรุป: ไม่มีชุดไหน
  ผ่านครบ 3 เกณฑ์** (มุมกล้อง desk-level rectilinear + axis-aligned bbox คุณภาพ eval
  + license เชิงพาณิชย์) ตัวที่ใกล้สุดเป็น overhead **fisheye ทั้งหมด**:

  | ชุด | ขนาด | ทำไมใช้ไม่ได้ |
  |---|---|---|
  | LOAF | 42,942 ภาพ / ~448K กล่อง | กล่องเป็น **radius-aligned rotated box** ไม่ใช่ AABB — แปลงแล้วกล่องบวม กด IoU; หน้า download เขียน MIT แต่ repo ไม่มีไฟล์ LICENSE และหน้า dataset เขียน "all rights reserved" (**ยังไม่ยืนยัน**) indoor เป็นส่วนน้อย 11 จาก 51 ฉาก |
  | CEPDOF / WEPDTOF | — | rotated box 5 ค่า `[cx,cy,w,h,d]`; WEPDTOF non-commercial **และตัดเฟรมจาก YouTube ของบุคคลที่สาม** BU จึงไม่มีอำนาจให้สิทธิ์เชิงพาณิชย์อยู่แล้ว; CEPDOF license ขัดกันเอง (non-commercial คู่กับ ODbL) |
  | PIROPO | >100,000 เฟรม | ground truth เป็น **จุดกลางศีรษะ** ไม่ใช่กล่อง — กู้ w/h ไม่ได้ |
  | IndoorCrowd | 9,913 เฟรม | **ห้ามใช้งาน surveillance โดยชัดแจ้ง** (ตรงกับงานนี้เป๊ะ) และทั้ง 4 ฉากเป็นพื้นที่สาธารณะในมหาวิทยาลัย ไม่มีโต๊ะทำงาน |
  | SCB-Dataset | — | label เป็น**คลาสพฤติกรรม** (ยกมือ/อ่าน/เขียน) กล่องครอบ "บริเวณพฤติกรรม" ไม่ใช่ทั้งตัว และ annotate เฉพาะคนที่แสดงพฤติกรรม → คนอื่นกลายเป็น FP |

  **ทางที่เหลือคือพึ่งตัวเอง:** ติดกล้องจริงแล้ว auto-label ด้วย OWLv2 /
  Grounding DINO แล้วตรวจมือ — เป็นทางเดียวที่ได้ครบทั้งมุมกล้องตรงโดเมน
  แสงจากจอ เก้าอี้ว่าง คนเดินผ่าน และสิทธิ์ใช้เชิงพาณิชย์
- **⭐ เพิ่มการรองรับ D-FINE ใน `desk_occupancy_multi.py`** — ตอนนี้ `PersonDetector`
  โหลดผ่าน `ultralytics` เท่านั้น ต้องเพิ่ม backend OpenVINO IR + processor
  (ยกโค้ดจาก `DFineDetector` ใน `benchmark/benchmark_models.py` มาได้เลย)
- **export `dfine-s`/`dfine-m` เป็น OpenVINO** — `dfine-m` ได้ถูกบัง 100% ที่ 8.13x
  ถ้าได้อัตราเร่ง 3.06x เท่า `dfine-n` จะเหลือ ~2.7x ซึ่งอาจรับได้บนเครื่องแรงหน่อย
- **วัดซ้ำด้วยภาพจากกล้องที่ติดจริง** โดยเฉพาะ `rtmdet-n-person` (22 ms, FP=0)
  กับ OpenVINO person-detection — ทั้งคู่เทรนกับกล้องมองลงมา ชุดทดสอบ COCO
  ปัจจุบันลำเอียงต่อต้านมัน ถ้าผลกลับทิศจะรันได้หลายสิบกล้องต่อ edge box
- ถ้ากล้องจริงแล้วพบว่าพลาดคนนั่งมุมยาก: ลอง `yolo11s` หรือโมเดลที่เทรนกับ
  มุมกล้อง overhead/fisheye โดยเฉพาะ
- **ByteTrack** (`model.track()` ใน ultralytics) — ไม่เพิ่ม recall ของเฟรมเดี่ยว
  แต่ track ยัง alive ตอน detector พลาด จึงลด flicker ของ state machine ราคาถูกมาก
  หลีกเลี่ยง BoT-SORT + Re-ID เพราะเก็บ appearance embedding = ระบุตัวบุคคลได้
  ขัดกับ privacy-by-design ในข้อ 3
- **ROI classifier แทน whole-frame detector** — กล้อง fix + โซน fix + ต้องการแค่
  `PRESENT`/`AWAY` จริงๆ คือ binary classification บน crop เล็กๆ
  (MobileNetV3-small 96x96 ≈ 1-3 ms/โซน = ถูกกว่า detector 30-100 เท่า)
  ต้องเทรนต่อการติดตั้ง — ใช้ YOLO เป็น teacher เก็บ label อัตโนมัติสัปดาห์แรกได้
- เก็บภาพ snapshot ตอนเกิด event เพื่อ audit (พร้อม privacy masking)

---

## 15. ประวัติการพัฒนา (สำหรับ context ต่อยอด)

1. เริ่มจากเวอร์ชันเต็ม `ACTIVE`/`IDLE`/`AWAY` (MediaPipe Pose + motion) — ใช้ทรัพยากรเยอะ (~85% CPU/กล้อง)
2. ผู้ใช้บอก "แค่อยากรู้ว่าทำงานอยู่มั้ย" → ลดเป็น `PRESENT`/`AWAY` (presence-only) ประหยัด 3–4 เท่า
3. เพิ่ม multi-camera/multi-zone + dashboard
4. ผู้ใช้บอกผลไม่แม่นยำ → สลับจาก HOG → MediaPipe Pose → YOLOv8n (แม่นยำสุด)
5. แก้บั๊กสำคัญ: ส่ง `dict` แทน `bool` ทำให้ทุกโซนกลายเป็น `PRESENT` เสมอ
6. เปลี่ยนกลยุทธ์จาก crop-ต่อโซน → ตรวจเต็มเฟรมครั้งเดียวแล้วแมปเข้าโซน (ลด false positive)
7. ผู้ใช้บอก "workstation fix ตำแหน่งอยู่แล้ว" → เพิ่มหน้าตั้งค่าโซนแบบลากเส้นบน snapshot + persist
8. เขียน `desk_occupancy_multi.py` ใหม่ทั้งไฟล์จาก spec ในเอกสารนี้ (2026-09-15)
   ทดสอบ 18 เคสผ่านหมด: state machine, endpoints ทั้ง 8 ตัว, calibrate + persist,
   และเคสบั๊กเดิม (ส่ง `dict` แทน `bool` — ตอนนี้ `Zone.update()` โยน `TypeError` ทิ้งเลย)
9. ติดตั้ง YOLOv8n จริงแล้วรันกับวิดีโอที่มีคนจริง (2026-09-15) — ยืนยัน false positive = 0
   บนโซนที่มีจอคอมแต่ไม่มีคน, เจอและแก้ 2 เรื่องระหว่างทาง:
   **OpenMP busy-wait** (CPU 334% -> 29%) และ **ชื่อโซนไทยขึ้นเป็น ???? บนวิดีโอ** (ใช้ Pillow วาดแทน)
10. เปรียบเทียบ 4 โมเดล (2026-09-15) แล้วเปลี่ยนตัวหลักจาก `yolov8n` -> **`yolo11n`**
11. สร้างชุดทดสอบจริงจาก COCO val2017 (46 ภาพ/131 คน) เฉพาะฉากที่คนถูกจอบังจริง
    พร้อม `benchmark/benchmark_models.py` — ผลกลับทิศจากการวัดด้วย 2 ภาพก่อนหน้า:
    `yolo11s` ดีกว่า `yolo11n` จริง (94% vs 88%) จึงลงไว้ทั้งคู่ให้สลับได้ใน config
    ระหว่างทางเจอว่าตัวกรองแรกนับ "คนที่ปรากฏบนหน้าจอทีวี" เป็นคนถูกบัง ต้องใส่เพดาน 60%
12. ลองโมเดลนอกตระกูล YOLO (2026-09-15): rtdetr-l, faster-rcnn x2, ssdlite,
    และ OpenVINO Model Zoo (person-detection-retail-0013 / -0202)
    ไม่มีตัวไหนชนะ yolo11s ในแง่ speed/accuracy บนชุดทดสอบนี้
    แต่เจอของดี: **export เป็น OpenVINO เร็วขึ้น 1.7-2.6 เท่าโดยความแม่นแทบไม่ตก**
13. ขยาย `benchmark/benchmark_models.py` ให้รองรับ 3 backend (ultralytics /
    transformers / onnx+openvino) แล้ววัด **D-FINE, RTMDet, CrowdHuman** (2026-09-15)
    ผล: **`dfine-n` ล้ม `rtdetr-l` ขาด** (แม่นเท่ากันแต่เร็วกว่า 2.4 เท่า) จึงกลับ
    ข้อสรุปเดิมที่ว่า "transformer ไม่คุ้ม" — เดิมวัดแต่รุ่นใหญ่สุดของตระกูล
    RTMDet แพ้ขาดตัดทิ้ง, CrowdHuman checkpoint จาก hub แย่กว่า `yolov8n` เปล่าๆ
    (สมมติฐานผิด — น่าจะเพราะนิยามกล่อง full-body ต่างจาก COCO)
    `yolo11s` ยังเป็นตัว deploy ต่อไป เพราะ `dfine-n` ยังไม่ได้ export OpenVINO
    บันทึกการรันเต็ม: `benchmark/example_run.md`
14. export `dfine-n` เป็น OpenVINO IR + INT8 แล้ววัดซ้ำ (2026-09-16) — **รอบที่เปลี่ยน
    ข้อสรุป**: OpenVINO เร่ง D-FINE ได้ **2.97 เท่า** (มากกว่าที่ YOLO ได้ 2.40 เท่า)
    ทำให้ `dfine-n` **เร็วกว่า `yolo11s` pytorch 1.76 เท่าและแม่นกว่า 3 จุด**
    โดย recall ตรงกับ PyTorch เป๊ะ — บทเรียน: อย่าเทียบโมเดลข้าม runtime
    INT8 ได้แค่ 1.22 เท่า (ไม่ใช่ 2-3 เท่าตามที่คาด) เพราะต้องกัน attention ไม่ให้
    quantize แต่ได้โมเดลเล็กลง 2.9 เท่า (15.9 -> 5.4 MB)
    ยังไม่เปลี่ยน production เพราะ FP สูงกว่า (4 vs 2) และ production ยังโหลด
    ผ่าน ultralytics เท่านั้น — เพิ่ม `benchmark/export_dfine_openvino.py`
    ทำให้ `yolo11s+openvino` เร็วกว่า `yolo11n+pytorch` และแม่นกว่าพร้อมกัน
    โค้ดรองรับการชี้ `yolo_model` ไปที่โฟลเดอร์ OpenVINO แล้ว
15. สร้าง**ชุดทดสอบที่ตรงโจทย์กว่าเดิม** `benchmark/build_desk_testset.py` (2026-09-16)
    16 ภาพคนนั่งที่โต๊ะ + **31 ภาพโต๊ะว่าง** (เคสที่ชุดเดิมไม่มีเลย) แล้ววัดซ้ำ 9 โมเดล
    — **รอบที่พลิกเกณฑ์ตัดสิน**: บนฉากโต๊ะจริง recall แทบไม่แยกโมเดล (92-100%)
    ตัวที่แยกแพ้ชนะกลายเป็น **false PRESENT** แทน `dfine-n` ที่เคยดูดีที่สุด
    เด้ง PRESENT บนโต๊ะว่าง 3/31 ใบ ส่วน `yolo11n` เด้ง 0/31 → **ไม่สลับ deploy**
    จูน `conf` ตามที่ค้างไว้ด้วย: `dfine-n` ที่ 0.6 ได้ FP ลดครึ่งฟรีๆ
    ระหว่างทางเจอว่าเกณฑ์คัดรอบแรกหลวมเกิน (มอเตอร์ไซค์/นักเทนนิสเสิร์ฟ/
    กรรมการบาสผ่านเกณฑ์ "ท่านั่ง" เพราะขายกทำให้สะโพก-เข่าใกล้กัน) ต้องตรวจ
    ด้วยตา 2 รอบแล้วรัดเกณฑ์ใหม่ — 46 ภาพเหลือ 16
16. ค้น dataset สาธารณะที่ตรงโจทย์แบบ deep-research (2026-09-16) — **ไม่มีชุดไหน
    ผ่านครบ 3 เกณฑ์** (มุมกล้อง / axis-aligned bbox / license เชิงพาณิชย์)
    ตัวที่ใกล้สุดเป็น overhead fisheye ที่ใช้ rotated box หรือ point annotation
    และเกือบทั้งหมด research-only สรุปว่าต้องสร้างชุดวัดจากกล้องตัวเอง (ดูข้อ 14)
